type Team {
  id: ID!
  name: String!
  channels: [Channel!]!
  isAdmin: Boolean!
}

type Query {
  teams: [Team!]!
}

type Mutation {
  createTeam(name: String!): Team!
  addTeamMember(email: String!, teamId: ID!): Boolean!
}
