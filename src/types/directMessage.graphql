type DirectMessage {
  id: ID!
  text: String!
  sender: User!
  receiver: User!
}

type Query {
  directMessages(teamId: ID!, userId: ID!): [DirectMessage!]!
}

type Mutation {
  createDirectMessage(receiverId: ID!, text: String!): DirectMessage!
}
